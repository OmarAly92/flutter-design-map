The workflow below applies **only when the user asks to map a Flutter app's
navigation, screens, or routes**, or asks for a visual sitemap of a Flutter app.
It is not general Flutter guidance — do not run any of it (and in particular
never boot a simulator or start a route sweep) unless that is what was asked for.

The parser CLIs referenced as `$PARSER` live in this extension's own checkout at
`packages/flutter_map_parser`; resolve that path relative to this file.

@./skills/flutter-design-map-skills/SKILL.md
