name: demo_navigator
description: Minimal Navigator 1.0 named-routes fixture for flutter_map_parser.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
