name: demo_navigator_generate_route
description: Navigator 1.0 fixture whose onGenerateRoute is a static method on a class in another file.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
